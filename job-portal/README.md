# 📌 Job Application Portal – React (Front-End Mini Project)

A complete front‑end mini project built using **React + Vite**, demonstrating:

* Form handling
* Validation
* Resume file upload
* LocalStorage data persistence
* Optional Mock API using JSON Server
* Routing (React Router)
* Listing, searching, and filtering applications

This project is perfect as a **portfolio mini‑project** or **learning assignment**.

---

## 🚀 Features

### 📝 **/apply – Job Application Form**

* Full form with:

  * Name
  * Email
  * Phone
  * Experience (years)
  * Resume upload (.pdf / .doc / .docx)
* Form validation:

  * Required fields
  * Email format check
  * Phone number validation
* Resume stored as Base64 Data URL
* On submit:

  * Save to LocalStorage (default)
  * OR send to JSON Server (optional)

---

### 📄 **/applications – Applications List**

* Lists all submitted applications
* Search by **name**
* Filter by **minimum experience**
* Delete application
* Download uploaded resume
* Export all applications as **CSV**
* Auto-loaded **prototype demo data** if empty

---

## 📂 Project Structure

This project uses a **single-file React setup**:

```
src/
 └── App.jsx    ← contains full project code
```

You can split it later if needed.

---

## 🛠️ Installation & Setup

### 1️⃣ Create a new React project using Vite

```bash
npm create vite@latest job-portal -- --template react
cd job-portal
npm install
```

### 2️⃣ Install React Router

```bash
npm install react-router-dom
```

### 3️⃣ Replace the contents of `src/App.jsx` with the provided code

Paste the entire file generated by ChatGPT.

### 4️⃣ Start the application

```bash
npm run dev
```

Your front-end is now running.

---

## 🌐 (Optional) Enable JSON Server for Mock API

By default, the project uses **LocalStorage**.
To use a fake backend, follow these steps.

### 1️⃣ Install JSON server

```bash
npm install json-server --save-dev
```

### 2️⃣ Create `db.json` in project root

```json
{
  "applications": []
}
```

### 3️⃣ Add script inside `package.json`

```json
"scripts": {
  "server": "json-server --watch db.json --port 4000"
}
```

### 4️⃣ Start mock backend

```bash
npm run server
```

### 5️⃣ Enable API mode

In App.jsx set:

```js
const USE_JSON_SERVER = true;
```

---

## 🎨 UI Overview

* Clean, minimal responsive layout
* Simple card-style UI
* Search & filter controls
* Download resume and CSV export

Demo prototype data auto-loads when LocalStorage is empty:

```
John Doe – 3 years
Priya Sharma – 5 years
Michael Lee – 2 years
```

---

## 📤 Exporting Applications (CSV)

The app provides a **one-click CSV export**:

* Includes id, name, email, phone, experience, resume name, applied date.
* Compatible with Excel, Google Sheets, etc.

---

## 🧪 How Data is Stored

### LocalStorage (default)

Key used:

```
job_applications
```

### JSON Server (optional)

Endpoint:

```
http://localhost:4000/applications
```

---

## 📦 Technologies Used

* **React** (front-end UI)
* **React Router** (client-side routing)
* **LocalStorage** (data persistence)
* **JSON Server** (optional mock backend)
* **Vite** (fast development & bundling)

---

## 🙌 Author

This project was generated and implemented with help from **ChatGPT**.

Feel free to extend the project with:

* Authentication
* Pagination
* Sorting
* Resume preview
* Dark mode

---

## 📧 Need Enhancements?

Ask ChatGPT to:

* Convert this to a multi-file structure
* Add Tailwind UI
* Add backend (Node/Python/Django/FastAPI)
* Deploy to Netlify/Vercel
